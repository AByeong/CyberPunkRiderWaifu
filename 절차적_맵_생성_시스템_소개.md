# 🗺️ 절차적 던전 생성 시스템 (Procedural Dungeon Generation System)

## 📋 개요

**CyberPunk Rider**는 플레이어에게 매번 새로운 게임 경험을 제공하기 위해 **실시간 절차적 던전 생성 시스템**을 도입했습니다. 이 시스템은 게임이 실행될 때마다 알고리즘을 통해 자동으로 독특한 레이아웃의 던전을 생성하며, AI 내비게이션, 조명 최적화, 적 배치까지 자동으로 처리합니다.

---

## 🎯 핵심 목표

1. **재플레이 가치 향상** - 매 플레이마다 다른 맵 구조로 지루함 방지
2. **개발 효율성** - 수작업 레벨 디자인 없이 무한한 던전 생성
3. **동적 난이도 조절** - 스포너 밀도와 경로 복잡도를 파라미터로 제어
4. **자동 최적화** - NavMesh 베이킹, 라이트 프로브 자동 생성

---

## 🏗️ 시스템 아키텍처

### 1. **Grid-Based Generation (그리드 기반 생성)**

던전은 **NxM 크기의 2차원 그리드**를 기반으로 생성됩니다. 각 그리드 셀은 9가지 타입 중 하나로 분류됩니다:

```
┌─────────────────────────────────────────┐
│  Grid Cell Types (그리드 셀 타입)      │
├─────────────────────────────────────────┤
│  Blank         │ 비활성 영역 (빈 공간)  │
│  Activate      │ 활성 바닥 (걸을 수 있음)│
│  Entry         │ 입구 (문)              │
│  Exit          │ 출구 (문)              │
│  Path          │ 연결 통로              │
│  Border        │ 벽 (경계)              │
│  CornerBorder  │ 코너 벽                │
│  Pattern       │ 장식 오브젝트 위치     │
│  Spawner       │ 적 스포너 위치         │
└─────────────────────────────────────────┘
```

### 2. **10단계 생성 파이프라인**

```
[1] 그리드 초기화
    ↓ NxM 배열 생성, 전체를 Activate로 설정

[2] 외곽 마스킹
    ↓ 가장자리 영역을 Blank로 처리 (플레이 가능 영역 제한)

[3] 경계 검출
    ↓ Blank와 인접한 셀을 Border로 설정

[4] 입구/출구 배치
    ↓ 경계선에서 랜덤하게 Entry/Exit 선택

[5] 경로 생성 (BFS 알고리즘)
    ↓ 모든 입구→출구 간 최단 경로 계산
    ↓ 50% 확률로 직선 경로 또는 중앙 경유 경로

[6] 고립 영역 제거
    ↓ Path에서 일정 거리 이상 떨어진 Activate 셀 삭제

[7] 경계 재검출
    ↓ 경로 생성 후 변경된 경계를 재계산

[8] 스포너 배치
    ↓ PatternDensity에 따라 랜덤하게 Spawner 배치
    ↓ Normal/Elite/Boss 스포너를 순차 할당

[9] 장식 패턴 생성
    ↓ 남은 Activate 셀에 장식 오브젝트 배치

[10] 3D 맵 구축
    ↓ 2D 그리드를 3D 프리팹으로 변환
    ↓ 벽/바닥/천장/문/장식물 인스턴스화
    ↓ 미니맵용 프리팹 생성
    ↓ NavMesh 베이킹
    ↓ Light Probe 자동 생성
```

---

## 🧠 핵심 알고리즘

### **BFS (Breadth-First Search) 경로 찾기**

입구와 출구를 연결하는 경로는 **BFS 알고리즘**으로 생성됩니다. 이는 최단 경로를 보장하며, 다음과 같은 특징이 있습니다:

```csharp
// 경로 생성 로직 (의사코드)
GeneratePath(start, end):
    if (50% 확률):
        // 직선 최단 경로 (단순한 맵)
        path = BFS(start → end)
    else:
        // 중앙점 경유 경로 (복잡한 맵)
        center = (width/2, height/2)
        path = BFS(start → center) + BFS(center → end)

    각 경로 포인트를 Path 타입으로 설정
```

**알고리즘 특징:**
- 4방향 이동만 허용 (상/하/좌/우, 대각선 X)
- Border와 Blank 셀은 회피
- Entry/Exit는 경로의 시작/끝으로 허용
- 시간 복잡도: O(W × H) per path

### **거리 기반 공간 필터링**

```csharp
RemoveFarActivateCells():
    for (각 Activate 셀):
        if (모든 Path 셀로부터의 거리 > pathProximityRadius):
            셀을 Blank로 변경  // 고립된 영역 제거
```

이를 통해 **플레이어가 도달할 수 없는 고립된 공간**을 자동으로 제거하여 깔끔한 레이아웃을 보장합니다.

---

## 🎨 3D 맵 빌드 프로세스

### **자동 회전 시스템**

벽과 문은 주변 그리드 상태를 분석하여 **자동으로 올바른 방향으로 회전**합니다:

```
┌─────────────────────────────────────┐
│  벽(Border) 회전 로직               │
├─────────────────────────────────────┤
│  왼쪽이 Blank  →  90°  (→ 방향)   │
│  오른쪽이 Blank → -90° (← 방향)   │
│  위쪽이 Blank   → 180° (↓ 방향)   │
│  아래쪽이 Blank →  0°  (↑ 방향)   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  코너(CornerBorder) 회전 로직      │
├─────────────────────────────────────┤
│  북서 코너 →   0° (ㄴ 모양)        │
│  북동 코너 →  90° (└ 모양)        │
│  남동 코너 → 180° (┌ 모양)        │
│  남서 코너 → 270° (┐ 모양)        │
└─────────────────────────────────────┘
```

### **프리팹 인스턴스화**

```csharp
BuildMap():
    for (x = 0; x < width; x++):
        for (y = 0; y < height; y++):
            position = (x * PositionOffset, 0, y * PositionOffset)

            switch (grid[x, y]):
                case Border:
                    fence = Instantiate(FencePrefab)
                    fence.rotation = GetFenceRotation(x, y)
                    // 10% 확률로 변형 벽 사용

                case Entry/Exit:
                    door = Instantiate(FenceDoorPrefab)
                    door.rotation = GetFenceDoorRotation(x, y)
                    path = Instantiate(PathPrefab)

                case Spawner:
                    spawner = GetNextSpawner()  // Normal→Elite→Boss 순서

                case Pattern:
                    prop = Instantiate(PropPrefabs[Random])
                    prop.rotation = Random(0°, 90°, 180°, 270°)

            // 모든 셀에 천장 생성
            // Path는 PathMinimap, 나머지는 GroundMinimap 생성
```

---

## ⚡ 자동 최적화 시스템

### **1. NavMesh 자동 베이킹**

```csharp
GenerateSubStage(int stageIndex):
    SubStageList[stageIndex].Generate()  // 맵 생성
    NavmeshSurfaceList[stageIndex].UpdateNavMesh()  // AI 경로 베이크
```

맵 생성 후 즉시 **NavMesh Surface를 베이킹**하여 적 AI가 올바르게 길찾기를 할 수 있도록 합니다.

### **2. Light Probe 자동 생성**

동적으로 생성된 맵에 맞춰 **라이트 프로브를 자동 배치**하여 실시간 조명 품질을 향상시킵니다:

```
[라이트 프로브 배치 전략]

1. 바닥 프로브
   - 모든 walkable 영역(Path, Activate, Entry, Exit)에 배치
   - 간격: probeSpacing (기본 4m)
   - 높이: 바닥 + 2m

2. 천장 프로브
   - Blank가 아닌 모든 영역에 배치
   - 높이: 천장 - 1m (기본 8m)

3. 벽 근처 프로브
   - Border 셀 근처에 추가 배치
   - 벽에서 1m 떨어진 위치
   - 4방향(상/하/좌/우) 검사

4. 전환점 프로브
   - Path와 다른 타입의 경계에 배치
   - 높이: 바닥 + 3m (1.5배 높이)
```

**Reflection을 이용한 동적 Grid 접근:**
```csharp
// private grid 필드에 접근하여 생성된 맵 정보 읽기
var gridField = typeof(GridGeneration).GetField("grid",
    BindingFlags.NonPublic | BindingFlags.Instance);
int[,] grid = (int[,])gridField.GetValue(gridGeneration);
```

---

## 🎮 게임플레이 통합

### **멀티 스테이지 시스템**

`StageManager`는 여러 서브 스테이지를 관리하며, 플레이어가 섹터를 클리어할 때마다 새로운 맵으로 전환합니다:

```
[게임 시작]
    ↓
StageInitialize()  // 모든 서브스테이지 생성
    ↓
SubStage[0].Generate()  // 첫 번째 맵 생성
    ↓
NavMesh 베이크
    ↓
스포너 활성화
    ↓
플레이어를 Entry에 배치
    ↓
[게임플레이]
    ↓
섹터 클리어 시
    ↓
MoveNextStage()
    ↓
SubStage[1].Generate()  // 새로운 맵 생성
    ↓
반복...
```

### **동적 스포너 할당**

```csharp
GetNextSpawnerPrefab():
    if (NormalSpawner 남음):
        return NormalSpawner
    else if (EliteSpawner[0] 남음):
        return EliteSpawner[0]
    else if (EliteSpawner[1] 남음):
        return EliteSpawner[1]
    else if (BossSpawner 남음):
        return BossSpawner
```

스포너는 **Normal → Elite → Boss 순서로 우선 배치**되어 난이도 곡선을 자연스럽게 형성합니다.

---

## 📊 성능 최적화

### **시간 복잡도**
- **전체 생성**: O(W × H × P)
  - W: 그리드 가로 크기
  - H: 그리드 세로 크기
  - P: 경로 개수 (entryCount × exitCount)
- **BFS 경로 찾기**: O(W × H) per path
- **고립 영역 제거**: O(W × H × P) (모든 셀에서 모든 경로까지 거리 계산)

### **공간 복잡도**
- **그리드 배열**: O(W × H)
- **BFS 자료구조**: O(W × H)

### **최적화 기법**
1. **Object Pooling 준비**: 스포너가 미리 배치되어 Enemy Object Pool 시스템과 연동
2. **단일 NavMesh Surface**: 각 서브스테이지마다 별도의 NavMesh 사용
3. **Light Probe Spacing**: probeSpacing 파라미터로 밀도 조절 가능
4. **Deferred Baking**: 맵 생성 후 0.1초 지연하여 라이트 프로브 생성 (안정성)

---

## 🎛️ 파라미터 기반 제어

개발자는 Unity Inspector에서 간단히 던전 특성을 조절할 수 있습니다:

```
┌────────────────────────────────────────────┐
│  조정 가능한 파라미터                      │
├────────────────────────────────────────────┤
│  width / height          │ 맵 크기 (20x20)│
│  entryCount / exitCount  │ 입출구 개수(2) │
│  maskMargin              │ 가장자리 두께  │
│  pathProximityRadius     │ 경로 근접 거리 │
│  PatternDensity          │ 장식 밀도 (10%)│
│  WallVariationChance     │ 벽 변형률 (10%)│
│  NormalSpawnerCount      │ 일반 적 개수   │
│  EliteSpawnerCount       │ 엘리트 적 개수 │
│  BossSpawnerCount        │ 보스 개수      │
└────────────────────────────────────────────┘
```

---

## 💡 기술적 효과

### **1. 개발 효율성 향상**
- **수작업 레벨 디자인 불필요**: 한 번의 시스템 구축으로 무한한 던전 생성
- **빠른 반복 테스트**: 파라미터 조정만으로 즉시 새로운 맵 생성
- **일관된 품질**: 알고리즘이 보장하는 플레이 가능한 레이아웃

### **2. 재플레이 가치 극대화**
- 매 플레이마다 **완전히 다른 맵 구조**
- 50% 확률의 경로 분기로 **예측 불가능한 레이아웃**
- 랜덤 장식 배치로 **시각적 다양성**

### **3. 자동화된 최적화**
- **NavMesh 자동 베이킹**: 수동 베이킹 작업 제거
- **Light Probe 자동 생성**: 실시간 조명 품질 보장
- **미니맵 자동 구성**: Path와 Ground를 구분하여 표시

### **4. 확장 가능한 구조**
- **모듈화된 파이프라인**: 각 단계를 독립적으로 수정 가능
- **타입 확장성**: `EGirdType` enum에 새로운 타입 추가 용이
- **프리팹 기반**: 아트 에셋 교체만으로 완전히 다른 비주얼 구현

---

## 🚀 실제 적용 사례

### **CyberPunk Rider에서의 활용**

```
[Lobby Scene]
    ↓
[Loading Scene]
    ↓
[Procedure Scene - 절차적 던전]
    ├─ SubStage[0]: 첫 번째 던전 (20x20)
    │   ├─ Normal Enemy Spawner x 5
    │   ├─ Elite Enemy Spawner x 2
    │   └─ 랜덤 레이아웃
    ↓
[Player Clear]
    ↓
[SubStage[1]: 두 번째 던전 (20x20)]
    ├─ 완전히 새로운 레이아웃
    ├─ Elite Enemy Spawner x 3
    ├─ Boss Spawner x 1
    └─ 더 복잡한 경로
    ↓
[Boss Fight]
    ↓
[Dungeon Clear]
```

### **실제 생성 예시**

```
■ = Blank (빈 공간)
□ = Activate (바닥)
# = Border (벽)
+ = CornerBorder
E = Entry (입구)
X = Exit (출구)
· = Path (경로)
S = Spawner (스포너)
P = Pattern (장식)

■■■■■■■■■■■■■■■■■■■■
■■■■■■■■■■■■■■■■■■■■
■■+###E####+■■+#####+■■
■■#·······#■■#□P□S□#■■
■■#·□□□··#■■#□□□□□#■■
■■#·□S□··#■■#□□□□□#■■
■■#·□□□··####□□□□□#■■
■■#·······□··□□□□P□#■■
■■+####·###+·+#######■■
■■■■■■#·■■■·■■■■■■■■
■■+####·###·########+■■
■■#□□□·□□·□S□□□□#■■
■■#□P□·□□·□□□□□□#■■
■■#□□□·□□·□□□P□□#■■
■■#□□□□□□·□□□□□□#■■
■■#□S□□□□·······X#■■
■■+#########+#####+■■
■■■■■■■■■■■■■■■■■■■■
■■■■■■■■■■■■■■■■■■■■
```

---

## 📈 향후 개선 방향

1. **룸 기반 생성**: 미리 정의된 방 템플릿을 조합하여 더 복잡한 구조 생성
2. **테마 시스템**: 바이옴별로 다른 프리팹 세트 적용 (사이버펑크, 감옥, 공장 등)
3. **난이도 곡선**: 플레이어 진행도에 따라 맵 복잡도와 스포너 밀도 자동 조정
4. **시드 기반 생성**: 특정 시드 값으로 동일한 맵 재생성 가능 (디버깅/공유용)
5. **멀티플로어**: 층별로 다른 높이의 던전 생성 (계단/엘리베이터 연결)

---

## 🎓 기술 스택 요약

```
┌─────────────────────────────────────────────┐
│  사용 기술                                  │
├─────────────────────────────────────────────┤
│  Unity 6000.0.48f1                          │
│  C# 그리드 기반 알고리즘                    │
│  BFS (Breadth-First Search)                 │
│  NavMesh Surface (AI Navigation)            │
│  Light Probe Group (Dynamic Lighting)       │
│  Reflection (Runtime Grid Access)           │
│  Object Pooling (Enemy Spawners)            │
│  Cinemachine (Camera Warping)               │
└─────────────────────────────────────────────┘
```

---

## 📝 결론

이 절차적 던전 생성 시스템은 **알고리즘적 접근**과 **게임 엔진 최적화 기술**을 결합하여, 개발 효율성과 플레이어 경험을 동시에 향상시킵니다.

**핵심 강점:**
- ✅ 완전 자동화된 맵 생성 파이프라인
- ✅ BFS 기반의 보장된 플레이 가능한 레이아웃
- ✅ NavMesh + Light Probe 자동 최적화
- ✅ 파라미터 기반의 유연한 난이도 조절
- ✅ 무한한 재플레이 가치

이를 통해 **CyberPunk Rider**는 제한된 개발 리소스로도 풍부한 게임 콘텐츠를 제공하며, 플레이어에게 매번 새로운 도전을 선사합니다.

---

## 📂 코드 위치

```
Assets/02.Scripts/Stage/
├── ProceduralGeneration/
│   ├── GridGeneration.cs          # 메인 맵 생성 로직 (802줄)
│   └── LightProbeGeneration.cs    # 라이트 프로브 자동 생성 (266줄)
└── StageManager.cs                # 스테이지 관리 및 전환 (177줄)
```

**총 코드량**: ~1,245줄 (주석 포함)

---

**작성자**: CyberPunk Rider Development Team
**작성일**: 2025-01-18
**버전**: Unity 6000.0.48f1
